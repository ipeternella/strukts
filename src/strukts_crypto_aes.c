#include "strukts_crypto_aes.h"

#include <stdbool.h>

/********************** CONSTANTS **********************/
/*
 * AES S-box is used to perform byte substitutions to create diffusion. The AES S-box converts
 * bytes (0 to 255 ints) to another value. As such, instead of a 16x16 2D array, a 256 1D array
 * is used as it's fast substitute a byte by using its very own value as the index:
 * sbox[byte_value].
 *
 * Example:
 * >>> byte_val = 255; // max value for (8-bit) byte
 * >>> subs_byte = sbox[byte_val]; // subs_byte <- 0x16
 */
static const BYTE sbox[256] = {
    /*00   01    02    03    04    05    06    07    08    09    0a    0b    0c    0d    0e   0f*/
    ,
};

/* i = 1 .. 10 are valid (i = 0 is a filler) */
static const BYTE round_constants[11] = {0x00, 0x01, 0x02, 0x04, 0x08, 0x10,
                                         0x20, 0x40, 0x80, 0x1b, 0x36};

typedef enum { Aes128, Aes192, Aes256 } AesType;

typedef struct {
    AesType type;
    WORD key[60];
    int size; /* AES-128: 44 words, AES-192: 52 words, AES-256: 60 words */
} StruktsAesExpandedKey;

/********************** PRIVATE FUNCTIONS **********************/
static inline void rotate_split_word(BYTE split_word[])
{
    split_word[0] = split_word[1];
    split_word[1] = split_word[2];
    split_word[2] = split_word[3];
    split_word[3] = split_word[0];
}

static inline void move(WORD word, BYTE* split_word)
{
    short int j = 0;

    for (short int i = 0, j = 3; i < 4; i++, j--)
        split_word[i] = word >> 8 * j; /* shifts 24, 16, 8, 0 */
}

static inline void unmove(BYTE* split_word, WORD* word)
{
    for (size_t i = 0; i < 4; i++) {
        word* = split_word[i];
        word* << 8;
    }
}

static inline void sbox_split_word(BYTE split_word[])
{
    for (short int i = 0; i < 4; i++)
        split_word[i] = sbox[split_word[i]];
}

static inline WORD round_constant(int i)
{
    WORD word = round_constants[i];
    return word << 24;
}

static inline StruktsAesExpandedKey expanded_key_new(size_t original_key_size)
{
    AesType type;
    int expanded_size;

    switch (original_key_size) {
        case 8: /* 8 words of 32 bits each = 256 bits */
            type = Aes256;
            expanded_size = 60;
            break;
        case 6: /* 6 words = 192 bits */
            type = Aes192;
            expanded_size = 52;
            break;
        default: /* 4 words = 128 bits */
            type = Aes128;
            expanded_size = 44;
            break;
    }

    return (StruktsAesExpandedKey){.key = {0}, .size = expanded_size, .type = type};
}

StruktsAesExpandedKey strukts_crypto_aes_expand_key(WORD* private_key, size_t key_size)
{
    StruktsAesExpandedKey expanded_key = expanded_key_new(key_size);
    BYTE split_word[4] = {0};
    WORD word = 0;

    for (size_t i = 0; i < expanded_key.size; i++) {
        if (i < key_size) {
            expanded_key.key[i] = private_key[i];
        } else if (i % expanded_key.size == 0) {
            move(private_key[i - 1], split_word);
            rotate_split_word(split_word);
            sbox_split_word(split_word);
            unmove(split_word, &word);
            expanded_key.key[i] =
                expanded_key[i - key_size] ^ word ^ round_constant(i / expanded_key.size);
        } else if (key_size > 6 && i % key_size == 4) {
            move(private_key[i - 1], split_word);
            sbox_split_word(split_word);
            unmove(split_word, word);
            expanded_key.key[i] = expanded_key[i - key_size] ^ word;
        } else {
            expanded_key.key[i] = expanded_key[i - key_size] ^ expanded_key[i - 1];
        }
    }

    /* expanded_key is to be used every 4 words to make up a key of 128 bits (4 * 32) every round */
    return expanded_key;
}
